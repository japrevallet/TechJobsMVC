@{ 
    ViewBag.Title = "Home";
}
<p>View jobs by:</p>
<ul>
    @foreach (KeyValuePair<string, string> action in ViewBag.actions)
    {
        <li><a href="/@action.Key.ToUpper()">@action.Value</a></li>
    }
</ul>